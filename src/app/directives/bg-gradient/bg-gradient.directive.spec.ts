import { BgGradientDirective } from './bg-gradient.directive';

describe('BgGradientDirective', () => {
  it('should create an instance', () => {
    const directive = new BgGradientDirective();
    expect(directive).toBeTruthy();
  });
});
